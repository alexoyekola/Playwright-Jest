import assert from 'assert';
// import the mocked mapper functions
// This is the function we are testing
// import rowMapper from '../../src/mapRow';

describe('#rowMapper', () => {
  it('should map', () => {
    assert.deepStrictEqual(true, true);
  });
});

